# Beads documentation source snapshot v1

Hypothesis-formation source, frozen 2026-08-23:

- repository: `gastownhall/beads`
- commit: `8d86c06bf231cbc0907436a111fb7b75d39ee12d`
- `docs/docs.json` blob: `fbd0bc97bfec1c5b2788e78e456eb20f6b3417c7`
- published page entries: **180**
- non-CLI published pages: **71**
- generated `cli-reference/*` pages: **109**

The page counts are source-selection expectations, not graph statistics. A real
corpus build still resolves every listed page inside the pinned checkout and
fails if any selected file is missing.

The ranking graph is derived only from hyperlinks authored inside the selected
Markdown/MDX bodies. `docs.json` navigation and redirects do not become citation
edges. Redirects may be used only to canonicalize the target of an authored
link.

Run both `published-human` and `published-all`. A centrality or degree-tail
observation that appears only after adding the 109 generated CLI pages should be
reported as generator-sensitive rather than as evidence about organic Memory
citation structure.
