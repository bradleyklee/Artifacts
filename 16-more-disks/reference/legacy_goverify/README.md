# Archived pre-v2 Go verifier

This is an incoming specialised `Q(sqrt(3))` dodecagon verifier from a prior
transfer packet. It expects the pre-v2 certificate layout and the old batch
policy. It is preserved only as historical source material; it is not compiled,
invoked, or authoritative in artifact 16.

The current authority is `cmd/lattice` / `internal/engine`, with independent
Python replay in `scripts/postcheck_go.py`.
