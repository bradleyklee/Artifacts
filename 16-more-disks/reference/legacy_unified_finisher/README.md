# Legacy unified-finisher material

These files came from the prior Python-centered unified-finisher package. They
are retained for comparative archaeology only. They use older naming and, in
some cases, the pre-v2 simultaneous-batch policy.

Do not treat `atlas/`, `certificates/`, `tools/`, or old renderer outputs here
as authority for artifact 16. The current authoritative tree is:

```text
cmd/ internal/ data/ check/ analysis/ renders/shorts/ docs/
```
