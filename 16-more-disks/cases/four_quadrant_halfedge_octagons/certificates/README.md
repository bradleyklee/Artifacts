# Certificate status

No self-contained per-event exact certificates were produced for the four-site
half-edge search. The files in `data/search/` are search/continuation records,
not independently checkable finite collision ledgers. Do not relabel them as
certificates.

The two initial conditions are copied verbatim below and are depicted in
`../figures/class_8_initial.svg` and `../figures/class_31_initial.svg`.
