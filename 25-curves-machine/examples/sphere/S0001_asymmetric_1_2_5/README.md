# Asymmetric Top 1-2-5

        Stable case ID: `S0001`  
        Status: `exact_certificate`

        Exact order-two asymmetric-top certificate on the unit sphere.

        ## Representations

        - `C01`: Ambient Cartesian sphere coordinates
- `C02`: Squared-variable invariant chart
- `C03`: Action-angle quotient chart

        ## Replay

        From the repository root:

        ```bash
        python3 examples/sphere/S0001_asymmetric_1_2_5/scripts/reproduce.py
        ```

        Filed replay commands:

        - `python3 algorithms/scripts/replay_even_sphere_asymmetric.py`

        Expensive commands are skipped unless `--full` is supplied.

        ## Imported sources

        - `branches/worktrees/sphere_curves/data/examples/sphere_curves/asymmetric_1_2_5_exact_certificate.json`
