# Circle-Triangle-Square

        Stable case ID: `P0004`  
        Status: `exact_certificate_showcase`

        One-page human certificate with exact differential checks, period data, Abel identity, and code-generated geometry.

        ## Representations

        - `C01`: Cartesian plane coordinates
- `C02`: Polar/action chart

        ## Replay

        From the repository root:

        ```bash
        python3 examples/plane/P0004_circle_triangle_square/scripts/reproduce.py
        ```

        Filed replay commands:

        - `python3 verify_certificate.py`

        Expensive commands are skipped unless `--full` is supplied.

        ## Imported sources

        - `certificates/raw/circle_triangle_square`
- `certificates/pretty/circle_triangle_square`
