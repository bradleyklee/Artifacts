# Paper

This directory is reserved for the project paper and build sources.

The supplied `25-curves-machine(1).zip` did not contain the desired current project note or an editable paper source. The external Platonic elliptic-surfaces paper used by the bridge work is retained under `references/`, but it is not relabeled as `note.pdf`.
