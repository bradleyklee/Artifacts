# Shared core

Only stable, branch-neutral utilities are shared at this stage: canonical JSON I/O, run capture, schemas, and verification plumbing. Mathematical reduction engines remain in their original backends until cross-backend tests justify extraction.
